train:
  description: Train linear regression model
  main: linearregression
  requires:
    - file: ../../data/sudoku-3m.csv
      target-path: data
  flags:
    alpha:
      arg-name: alpha
      default: 1.0